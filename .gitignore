# unreal-engine-gitignore
#
# 此 .gitignore 文件已根据项目结构进行了配置
# 项目根目录: ./
# Unreal项目目录: ./ChenXi_VisualNovel/
# ----------------------------------------------------------------------

# ----------------------------------------------------------------------
# 核心Unreal Engine排除项 (所有UE项目必需)
# 注意：所有路径都已添加 "ChenXi_VisualNovel/" 前缀
# ----------------------------------------------------------------------

# 1. 可以被引擎或IDE完全重新生成的二进制文件。
/ChenXi_VisualNovel/Binaries/
/ChenXi_VisualNovel/Plugins/**/Binaries/

# 2. 中间构建数据和临时文件。
/ChenXi_VisualNovel/Intermediate/
/ChenXi_VisualNovel/Plugins/**/Intermediate/

# 3. 保存的文件：日志、配置缓存、自动保存和用户特定数据。
/ChenXi_VisualNovel/Saved/

# 4. 引擎的缓存目录 (绝不应提交)。
/ChenXi_VisualNovel/DerivedDataCache/

# 5. 构建产品和部署目录
/ChenXi_VisualNovel/Build/
/ChenXi_VisualNovel/Packaged/
/ChenXi_VisualNovel/Releases/

# 6. 本地化缓存/导出文件
/ChenXi_VisualNovel/Content/Localization/**/*.csv
/ChenXi_VisualNovel/Content/Localization/**/*.po
/ChenXi_VisualNovel/Content/Localization/**/*.archive

# 7. 故事文件，目前尚无正式版本，不上传
/StoryData

# ----------------------------------------------------------------------
# IDE & 系统文件 (这些规则是全局的，无需修改)
# ----------------------------------------------------------------------

# Visual Studio & IDE files
.vs/
.vscode/
*.sln
*.suo
*.user
*.ncb
*.aps
*.pdb
*.opensdf
*.sdf
*.ipch

# macOS/Linux metadata
.DS_Store
.Trash-*
.directory

# ----------------------------------------------------------------------
# 生成的文件 (这些规则是全局的，无需修改)
# ----------------------------------------------------------------------

# 由引擎生成的解决方案/项目文件
*.xcodeproj
*.xcworkspace
*.VC.opendb

# 可执行文件和库 (应由 /Binaries/ 排除项处理，但为了安全起见再次列出)
*.dll
*.exe
*.obj
*.lib
*.pdb
*.so
*.dylib
*.a

# ----------------------------------------------------------------------
# Git LFS & Gitee 兼容性
# ----------------------------------------------------------------------

# 你应该提交 *.uasset 和 *.umap (你的核心内容)。
# 如果Gitee的LFS限制严格，它们必须作为普通文件进行跟踪
# 通过移除LFS跟踪，如之前的步骤所示。
# 以下行为安全/调试目的，但核心内容不应被忽略。
# *.uasset
# *.umap

# 忽略之前gitignore中常见的大型压缩文件
*.zip
*.rar
*.7z
*.mp4
*.wmv
*.wav
*.mp3
*.ogg